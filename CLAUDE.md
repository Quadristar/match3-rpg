# CLAUDE.md(3マッチパズル＋RPG)

このファイルは、このリポジトリで作業する Claude Code が毎回守るべきルールです。
作業を始める前に必ず読み、あわせて次の2つも確認してください。

- `docs/ARCHITECTURE.md` … 共通基盤の設計(雛形 alcyon-game-template から引き継いだもの)
- `docs/GAME_DESIGN.md` … このゲーム固有の設計

---

## 1. このリポジトリについて

- ブラウザで動作する 2D の「3マッチパズル＋RPG」ゲーム(完全新規開発)
- 雛形リポジトリ `alcyon-game-template`(v1.0.0)から作成した
- 技術: TypeScript / Vite / PixiJS / Vitest / ESLint
  - GSAP・Howler.js は演出の段階で導入を検討する。導入するときは §3-7 に従い、報告してから追加する
- 対象: PC・スマートフォン・タブレット(縦画面・横画面の両対応、縦を優先)
- 前提とする表現: 高品質な静止画(立ち絵・背景)を、プログラム側の演出で魅力的に見せる 2D ゲーム
- 世界観・キャラクター・ストーリー・パズルの細部ルールは**未確定**

### 開発者の環境(重要)

- 開発者は**スマートフォンのみ**で開発しており、PC を使っていない
- 動作確認は、**GitHub Pages に公開されたビルドをスマホのブラウザで開いて**行う(https://quadristar.github.io/match3-rpg/)
- PC のブラウザの開発者ツールを前提とした確認手順は指示しないこと
- 画面なしで検証できることは、テスト(Vitest)と型チェックで保証すること

---

## 2. 雛形との関係

- `core/` / `services/` / `app/` と汎用の `presentation/`(`ui/`・`debug/`・`loading/`・`scenes/Scene.ts`、今後追加する `effects/`)は**共通基盤**。変更は慎重に行い、変更した場合は報告の「共通基盤への変更」に明記する
- 共通基盤への変更のうち、他のゲームでも役立つもの(バグ修正・汎用機能)は、報告の「雛形への還元候補」に挙げる
- ゲーム固有の処理を共通基盤に混ぜない。ゲーム固有の処理は `systems/` / `data/` / ゲーム用の `presentation/`(`game/`・`scenes/<シーン名>/`・`views/`)に置く
- **依存ライブラリは最小限にする。**
- **変更は `CHANGELOG.md` に記録する。** 雛形から取り込んだ変更も、雛形のバージョンとあわせて記録する
- 雛形を更新したときは、雛形の `CHANGELOG.md` で README に記録したバージョンより新しい変更を確認し、必要なものを手動で取り込む(`docs/NEW_GAME.md` の「雛形を更新したとき」)

---

## 3. 最重要ルール

1. **変更前に既存コードを読む。** 現在の設計を把握してから作業する。
2. **小さく変更する。** 1回の作業は1つの目的に絞る。依頼と無関係なファイルは変更しない。
3. **既存機能を全面的に書き換えない。** リファクタリングが必要だと判断した場合は、実施する前に理由と影響範囲を報告し、承認を得る。
4. **設計を勝手に変えない。** 問題が起きたら、まず原因を特定して報告する。別の設計に切り替えるかどうかは開発者が判断する。
5. **仕様を勝手に決めない。** 進めるために必要な仮決めは、コードのコメントと報告の両方で「仮仕様」と明記する。
6. **仕様の矛盾や技術的な問題は指摘する。** 指示に単純に従うのではなく、懸念点を先に伝える。
7. **依存ライブラリを勝手に追加しない。** 追加が必要な場合は、理由・代替案・容量への影響を報告してから追加する。
8. **作業の最後に `npm run check` を通す。** 通らない状態で作業を終えない。
9. **HTTP 403 などの権限エラーが出た場合は再試行しない。** その時点で作業を止めて報告する。

---

## 4. アーキテクチャと依存ルール

詳細は `docs/ARCHITECTURE.md`(共通基盤)と `docs/GAME_DESIGN.md`(ゲーム固有)を参照。ここでは守るべき境界だけを記す。

| ディレクトリ | 役割 | import してよいもの |
|---|---|---|
| `src/core/` | 汎用基盤(EventBus, StateMachine, SeededRng, ObjectPool など) | 外部依存なし |
| `src/data/` | キャラ・敵・ステージ・パネル効果などの型付きデータ | `core` |
| `src/systems/` | **純粋なゲームロジック**(パズル・バトル・成長) | `core`, `data` |
| `src/services/` | アセット・音声・セーブ・入力・設定・レイアウト | `core`, 外部ライブラリ |
| `src/presentation/` | 描画・演出・UI・シーン | `core`, `systems`(主に型), `services`, 外部ライブラリ |
| `src/app/` | 起動処理、シーン管理、各層の接続(ロジックと演出の橋渡しを含む) | すべて |

**絶対に守ること:**

- `core` / `data` / `systems` から `pixi.js` / `gsap` / `howler` / `services` / `presentation` / `app` を import しない(ESLint でエラーになる)
- `systems` 内で `Math.random()` と `Date.now()` を使わない。乱数は `SeededRng` を引数で受け取る
- パズルは「**先に連鎖の最後まで計算して結果を返し、描画側がそれを再生する**」方式を守る。ロジックがアニメーションの完了を待つ作りにしない
- パズルとバトルを直接結びつけない。両者の接続は `app/` の BattleDirector だけが行う
- パネルが消えたときの効果(ダメージ・回復など)の変換規則はコードに書かず、`data/tileEffects` に置く

---

## 5. コーディング規約

- TypeScript は `strict`。`any` は使わない(やむを得ない場合は理由をコメントに書く)
- アセットのファイルパスをコードに直接書かない。必ずマニフェストのキー(型付き定数)を使う
- 調整用の数値をロジック内に直接書かない。データまたは設定に置く(ゲームバランスに関わる数値 — ダメージ係数・HP・ターン数など — は `data/` に置く)
- 画面上の座標をビューに直接書かない。レイアウト定義の領域・基準点から配置する
- 1ファイル1責務。ファイルが300行を超えそうなら分割を検討し、報告する
- 生成した Pixi オブジェクトやテクスチャは、シーン終了時に確実に破棄・解放する
- コメントとドキュメントは日本語で書く

---

## 6. 画面の向き(縦・横)のルール

- 縦画面と横画面の両方に対応する
- 向きの判定と各領域の配置は `services/layout` の**レイアウト定義**が一元的に担当する(ゲームの定義は `presentation/game/gameLayout.ts`)
- ビューや演出は、レイアウト定義が提供する**領域と基準点**(「盤面の領域」「敵の位置」「盤面の中心」など)を参照して配置する
- 演出中に画面が回転したら、再生中の演出を即座に完了させてから配置し直す(ロジックの状態は別に保持しているため壊れない)
- 作り込みは縦画面を優先する。すべてのシーンで、縦・横の両方で崩れない(横画面は「崩れずに遊べる」)ことを常に維持する

---

## 7. テスト

- `core/` と `systems/` を変更したら、対応するテストを追加または更新する
- 不具合を修正するときは、まず不具合を再現するテストを書く
- シード付き乱数を使い、同じ入力なら同じ結果になることを保証する
- データ同士の参照(ステージ→敵IDなど)の整合性はテストで検査する

### コマンド

```
npm run dev        # 開発サーバー
npm run build      # 本番ビルド
npm run test       # テスト
npm run lint       # lint(依存境界の検査を含む)
npm run typecheck  # 型チェック
npm run check      # typecheck + lint + test + build をまとめて実行
```

---

## 8. Git 運用

- `main` は常に動く状態に保つ。直接コミットしない
- 作業はブランチで行う(例: `feat/phase3-match-finder`, `fix/cascade-refill`)
- Claude Code のセッションでブランチ名が指定されている場合は、その名前を使ってよい
- コミットは小さく分け、メッセージは `feat:` / `fix:` / `refactor:` / `test:` / `docs:` / `chore:` で始める
- 大きな変更の前には、その時点の状態をコミットしておく
- PR を作成し、CI(型・lint・テスト・ビルド)が通ってからマージする
- `main` にマージされると GitHub Pages へ自動公開される

---

## 9. 作業報告のテンプレート

作業の最後に、必ず次の形式で報告すること。

```
## 作業報告

### 何を変更したか

### なぜ変更したか

### ファイル
- 追加:
- 変更:
- 削除:

### 動作確認
- npm run check の結果:
- 追加・更新したテスト:
- スマホ(GitHub Pages)で確認してほしい点:

### 仮仕様として決めたこと

### 共通基盤への変更(あれば)

### 雛形への還元候補(あれば)

### CHANGELOG への記載内容

### 残っている問題・懸念

### 次の作業の提案
```

---

## 10. 現在のフェーズ

- 現在: **開発準備完了(Phase 3a 着手前)**。次は Phase 3a(パズルロジック＋テスト)
  - Phase 1〜2 は雛形リポジトリで完了済み(雛形 v1.0.0)
  - 開発準備: デモの削除、最小の形と仮のタイトル画面、ドキュメントの整備(完了)
- フェーズの一覧と内容は `docs/GAME_DESIGN.md` の「開発フェーズ」を参照
- フェーズが進んだら、この項目を更新する
